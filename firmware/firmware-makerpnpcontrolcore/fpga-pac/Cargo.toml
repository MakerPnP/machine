[package]
name = "fpga-pac"
version = "0.1.0"
edition = "2024"

[features]
default = ["defmt"]

defmt = [
    "dep:defmt"
]

[dependencies]
defmt              = { version = "1.0.1", optional = true }
